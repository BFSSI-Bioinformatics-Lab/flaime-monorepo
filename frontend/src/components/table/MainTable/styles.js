import { TextField } from "@mui/material";
import { styled } from "@mui/system";

export const TableActionButtonsContainer = styled("div")(() => ({
    marginBottom: 20
}));
export const TableSearchBar = styled(TextField)(() => ({
    paddingBottom: 15
}));